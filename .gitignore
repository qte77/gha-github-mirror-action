# Sandbox artifacts (Claude Code worktree bare git files)
/HEAD
/hooks
/objects
/refs

# Claude Code local (user-specific, not shared)
.claude/settings.local.json

# Local secrets / env (never commit)
.env
.env.*

# Local clone destination for scripts/clone-local.sh
mirrors/
